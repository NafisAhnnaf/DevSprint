apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus      # Added this UID to match your Dashboard JSON
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "5s"